"use client";

import { Campaign } from "@/types";

export const Withdraw = ({
  campaign,
  userAddress,
}: {
  campaign: Campaign;
  userAddress?: string;
}) => {
  return <div>Withdraw money</div>;
};
